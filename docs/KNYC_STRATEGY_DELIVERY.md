# KNYC 三策略持续信号交付

2026-09-19 用户批准实施。本文件记录目标，完成证据另记 CURRENT_STATE.md；未通过项不得勾选。

## 固定边界

现有 VM 规格、磁盘及基础设施配置不变。复用采集、KNYC 特征、Nautilus 和统一终端，先查重复计算、写盘放大、阻塞查询和积压。Kalshi 优先、Poly US 其次，排除 Poly Intl，保留 KLGA 历史。凭据仅服务端按需读取已有 KALSHI.txt/POLY.txt，不打印、不入库、不生成 SSH 密钥。Live 默认关闭，Agent 不发送真实订单；用户手动验收每平台累计支出上限 5 USD，包含费用和挂单预留，回款不恢复额度。批量删除先列精确目标并人工审核。

## 执行版 v2

- 时间窗为纽约 12–22 点。S1/S3 结束升温概率达到 90%；历史钟点只作特征，不能确认当天高点。
- S1 取联合概率最大的相邻两档；各至少一份并满足平台最小数量，在费用、预算、深度和共享风险约束内最大化期望净收益。并列取低成本，再按固定数量顺序确定。无正优势则 no-trade。
- S2 距上档一个结算温度单位内预警；收到实际新高跨档且新档概率至少 90% 才交易。S3 最大概率单档一份，平台最低数量更高则拒绝。
- 天气触发与交易机会分开持久化，报价/天气/风险变化可重评，当前条件仍须有效。每账户/模式/平台/市场日/策略最多建立一次目标仓位；任何部分成交消耗机会，S1 残腿停止补单。未知订单先对账，明确零成交终结后才可重评。
- 关闭自动交易仍生产信号。共享逻辑接入回放、Paper、Live；保存版本、证据时间、概率、数量、成本、结果损益和拒绝原因。v1 保留作历史对照。

## 交付核验

- [ ] 双平台规则及结算证据，窗口/舍入/修订歧义维持 no-trade。
- [ ] KNYC 策略概率校准和持续生产；不补造历史 received_at。
- [ ] v2 信号、优化、重评、持久化和真实数据模拟闭环。
- [ ] Nautilus Live 原生报告、账户认证和完整对账，终端账户/白名单/风险控制。
- [ ] 超时未知、重复请求、部分成交、重启和回滚验证。
- [ ] 原 VM 线上首屏 30 样本 p95≤3s；切档/本地反馈各100样本、消息渲染500样本 p95≤100ms。
- [ ] 最终版本完整纽约市场日及最终结算/对账；无持续积压、无 OOM、无数据丢失。
- [ ] required checks 通过、PR 审查合并、精确 SHA 部署、manifest 和运行记录一致。

普通实现、测试、审查、修复、PR、合并、SSH 普通重连、部署和观察持续执行，无需逐步确认。真实市场日、结算或用户交易证据尚未出现时保持待验收。
