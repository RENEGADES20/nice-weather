# KNYC 终端迁移：本地验收记录

状态：部分实现，通过下面列出的本地检查；完整交付未完成。工作基线 `fc83189ae168ffcdc386ee714a8fe89cb846930d`，隔离分支 `codex/knyc-terminal`，根目录既有改动保留。

## 检查事实

- 本地交易测试：66 passed；另 2 项 Redis 恢复测试在 Windows 缺 redis-server，已由 PR #45 的 `667cd588` Linux Nautilus CI 补齐通过。新增 7 项覆盖冻结选择、无未来输入、来源不混用、深度/费用、原文版本、分页、认证/CSRF、幂等请求及原生策略恢复。S1 部分成交用先前订单消耗深度构造，验证只提交第一腿并在重启后不重试。
- Python ruff 与仓库高置信度密钥扫描通过。TypeScript 与 Vite 构建通过。
- Playwright：1440×1100 桌面、390×844 手机布局检查；6 档，每档 1500 历史点，24 次切换/命令/行情推送。缓存切档 p95 28.8 ms，命令本地反馈 39.5 ms，消息到显示 29.2 ms。首屏 628 ms，只有一次样本，不能称为首屏 p95。原始本地测量见 `knyc-terminal-local-2026-09-19.json`。
- 测试网络为本机 Vite 开发服务器及拦截的合成 API/WebSocket；不含 VM、交易所和真实网络延迟，不证明整日稳定性。截图保留在本地 Playwright test-results，未发布私有页面。
- 实际公开采集已取得 Kalshi 与 Poly US 各 6 个 KNYC 档位/盘口、METAR 与 CLI；HRRR 一轮 18 个时效成功提取 KNYC 格点 index 1257255。各请求保存真实请求/接收时间，原文按 SHA256 去重；HRRR 保存索引、字节区间、字段哈希和格点结果。

## 审查修复

快照与游标使用一致数据库事务；NO 票据联动互补盘口；历史分页有缓存上限；实时新收到行情不使用上一秒 UI 时钟判为未来；回测请求 ID 冲突返回 409；不同平台/日期/结算源和重复档位拒绝；S1 每腿等待原生 IOC 结果后才决定后续下单。

## 阻塞和未完成

Kalshi 当前规则源为 The Weather Company，Poly US 为 NWS CLI。观察窗口、舍入及最终修订规则仍需完成逐平台审计，实际公开合约一律标记 ambiguous / no-trade。现有 KNYC 研究模型缺真实历史 received_at 和 HRRR，未将其伪装成生产模型；无模型回放明确 no-data。

实盘认证/执行/对账、用户风险与账户启用、原生平台行情订阅、US 结算事件、生产模型及完整日观察仍未交付。VM 控制台显示实例运行，但浏览器 SSH 无法核验命令输出，运行 SHA 尚未确认；未更改生产服务或撤下 Streamlit。完整验收清单保留未勾选。

CI 证据：[Linux 验证](https://github.com/RENEGADES20/nice-weather/actions/runs/35425951757)。PR #45 最终提交 `39924ee6` 的七项检查全部通过后合并；main 提交 `e00ac3cecb0cc262b86dafa6416d91785632f5b8`。该记录不代表 VM 已部署。
# 2026-09-19 请求恢复增量验收

网络响应丢失后，终端保留原 ID/参数并暂停新增订单；刷新和另一标签页恢复待核验记录。原 ID 重试前查询精确回执，修改当前票据不会改变旧请求；202 排队回执不解除门禁，accepted/rejected 才解除。停止策略和撤单仍可操作。后端精确查询已验证可以读取最近 30 条列表之外的记录。

本机验证：8 项 Python 终端/策略回归通过；两个 Playwright 场景通过，随后补充跨标签页恢复断言并单独复验通过；TypeScript/Vite、Ruff、差异检查和密钥扫描通过。6 档×1500 点合成场景的 24 样本切档、命令反馈和收到行情至显示均通过 p95≤100ms 断言，首屏仅单次≤3s，不能表述为首屏 p95 验收。未执行真实资金交易，未验证 VM 网络或完整市场日。

本机采集因电脑重启中断，最后记录 09:01:34 UTC，15:18 UTC 恢复追加；缺口保留。VM 已核验旧清单 fc83189，发布文件尚未上传，当前等待已登录 SSH 来源权限；不得把本地测试写成已部署。
