# Nautilus 工作台本地验收报告

日期：2026-09-12。结论：本地 Trading / Backtest 核心可操作，整体计划尚未完成验收。未启用真实交易、未读取真实凭据、未发生真实资金操作。

## 实现与版本

- 隔离分支：`codex/nautilus-trading-workbench`，远端主分支基线 `e827138f9535996ed0a2162d14bcd9997af2b2b8`。原工作目录未提交内容保留。
- 引擎实现提交：`109e9916af6272221ed283280c3d9b6ac80fce4d`；固定回测提交：`98fbcb4677550714d348e0063382c3ca9ec04d23`；随后 `5e61d67` 仅将比较页面的指标改为两列，解决窄屏数值截断。
- 实际执行环境：Ubuntu WSL，Python 3.12.13、Nautilus Trader 1.231.0、PyArrow 25.0.1。Windows 原生扩展被系统应用程序控制阻止，未绕过系统防护。
- 独立模拟、回测 worker；Dashboard 写独立请求库并只读结果。保留原五个页签和旧 Shadow/Paper。主采集库增加实际 NO token，旧 Repricing 查询显式限制 YES。
- 示例策略为验收工具，未声称交易优势。阶段 A 策略与 Repricing 延迟优化后置。

## 可复核结果

### 桌面与移动端模拟交易

公开市场：KLGA 2026-09-12、78–79°F、NO token `50192766407086588969542653315447082504846473034012401969891471624647008655130`。

同一模拟账户 `sandbox-001`，会话 `eeebfc15-8566-4a6f-bec0-37c44a96dc88`，初始现金 100 pUSD。

| 项目 | 结果 |
| --- | ---: |
| 桌面买入 / 退出 | 5 份，0.35 / 0.34 |
| 移动端买入 / 退出 | 5 份，0.35 / 0.34 |
| 原生成交总数 | 4 |
| 最终持仓 | 0 |
| 最终现金 / 净值 | 99.67405 |
| 累计原生费用 | 0.22595 |
| 已实现 / 总 P&L | -0.32595 |
| 最大回撤 | 0.32595% |

两次交易均通过浏览器表单提交，由实际 Nautilus BacktestEngine 撮合。报价来自公开自采盘口和操作前的公开只读盘口刷新。这里的成交与 pUSD 余额属于模拟环境。

成交后重启、退出后重启均使用原会话日志。最终恢复核对：现金、持仓、订单、逐笔成交、净值、最大回撤全部一致。`account-and-runs.json` 保存逐项布尔核对结果；`before-final-restart.json` 保留修正前的展示证据。

### 自采数据回测

- 接收窗口：`2026-09-12T05:38:00+00:00` 至 `2026-09-12T05:58:23.542591+00:00`。
- 数据集：`710fb59e0ad0a73ad5532bd0f8f5c076599d579bee7ce26e8789127f53d90323`，共 4,678 个输入事件。worker 仅回放选定市场和双侧约束要求的配对行情。
- 参数：`acceptance_roundtrip`，quantity=5、exit_after_quotes=3、require_both=true；初始现金 100。
- 已观察的完整回测结果：2 笔成交、持仓归零、净值 99.7895、费用 0.1105、已实现亏损 0.2105、最大回撤 0.2105%。最终固定版本两次运行 ID、配置哈希及快照一致性见 `account-and-runs.json`。
- 最终比较运行：`c1c6a167-0f95-47bc-95a2-7829bdbb11d9` 与 `05e148c3-8ea9-4ad2-ad82-72ba795073d0`；配置哈希、源码哈希及完整快照哈希均一致。
- 取消记录 `ff98e1d0-fbd3-4cce-a866-fd61dc25be9b` 保留为 cancelled，页面明确显示部分结果，未覆盖完成记录。

## 验证范围

| 类别 | 已验证范围 |
| --- | --- |
| 原有 Python 回归 | 单元、集成、Dashboard；空库及没有旧 Runner 决策仍能打开新页 |
| 原生交易 | YES/NO、买卖、部分成交、流动性消耗、撤单、撤改单剩余目标数量、退出、0/1 结算 |
| 风控 | 无裸卖、资金及敞口、非法精度、陈旧/缺失报价、规则歧义、GTD 过期、FOK 深度不足、post-only 非法组合 |
| 时间与数据 | 数据内容哈希、旧覆盖表不改变历史规则、缺失 NO 历史拒绝、双侧报价在决策时点齐备、纽约夏令时偏移 |
| 恢复 | 成交后恢复、已记输入未记结果的中断、重复请求/回放、篡改快照暂停、旧统计口径升级后再次恢复 |
| 官方客户端 | 实际官方执行客户端通过受控 HTTP 注入认证失败及请求超时；未发送真实订单 |
| 前端 | 类型检查、14 项单元测试、生产构建；桌面和移动端表单操作；窄屏实测 CSS 宽度 325px，无横向溢出 |

Python 最终 **111 项全部通过**，完整输出保存在 `pytest.txt`；回归依赖有 NumPy timedelta 弃用警告，不影响本次测试结果。前端 14 项测试、类型检查及构建通过。前端依赖安装报告两个 moderate 级历史依赖告警，本轮未执行破坏性依赖升级。Ruff、diff 空白检查及项目密钥扫描通过。

浏览器已确认 JSON 和成交 CSV 的下载事件。JSON 下载后的页面读取曾长时间阻塞；重新连接同一标签页后完成 CSV 验收。2026-09-12 14:52 UTC 页面再次显示模拟 worker 已连接、现金 99.67405；这项事后状态核对不代表期间已完成连续可用性验收。LIVE 已核验为 Disconnected / Live equity unavailable，无真实订单表单。最终读取的浏览器错误日志为空。

## 验收中发现并修正的问题

1. 原入口在没有旧决策时提前返回：先创建七个页签，使两个新工作台独立于旧 Runner。
2. 合约展示表覆盖历史版本：数据集改为从不可变原始 capture 重建规则和费用。
3. 采集积压旧报价覆盖新报价：实时入口拒收超过 30 秒或早于当前 token 报价的积压记录，原采集历史保留。
4. 没有新行情时 GTD 不到期：启用 Nautilus 原生 GTD 定时管理。
5. NETTING 重新开仓后累计费用与盈亏丢失：统计版本 2 汇总原生持仓历史快照。旧日志仍按版本 1 核对，随后追加 `projection-upgrade-2` 事件；现金、持仓和成交没有被改写。
6. 官方 v1.231.0 连接超时将字符串错误当字典读取：仅在该客户端实例的连接入口保留原始 PolyApiException，保持未连接状态，没有全局补丁。
7. 回测列表新增/排序变化导致比较选择丢失：比较控件增加稳定标识。
8. 提交前审核发现存量挂单缺少完整市场有效性检查：复用新订单的共同检查，在规则歧义、费用未知、市场停用或观测窗口结束后，先撤单再允许新行情进入撮合。原生撤单进入处理队列，必须先推进引擎处理撤单，再送入新报价；新增五个场景覆盖市场失效及陈旧报价后的撤单顺序。
9. 首轮远端 CI 的两个前端场景超时：旧测试遍历全页单选框，将新 Trading 页的 SANDBOX/LIVE 误认为温度档位。测试定位改为 Repricing tabpanel 内，保留全部温度档位切换及断线恢复断言。

失败与过期请求均保留在验收包，包含首次缺失可执行报价被拒绝、积压时请求过期和取消的部分回测。

## 尚未通过的整体完成门槛

- **正式 Ubuntu 主机的完整 KLGA 市场日运行**：尚未获得部署连接信息；本地短时 WSL 观察不能代替。systemd 文件已提交，正式主机部署、采集/R2 连续不受影响及完整日证据待完成。
- **官方真实执行客户端完整故障矩阵**：HTTP 认证失败和超时已测；提交结果未知、WebSocket 重复成交报告、Redis 重启对账的完整组合尚未验收。LIVE 目前交付官方客户端/TradingNode/Cache 配置接线及禁用保护，真实账户监控、外部资产对账与资金限额启用没有开放。
- **归因与数据覆盖**：原生 NETTING 持仓显示统一 Workbench 策略 ID，订单有 manual/strategy 归属；多策略独立持仓归因、外部充提现金流调整、既有真实资产均未验收。尚无 30 个完整日，因此 Sharpe 不可用。
- 自然最终结算尚未观察到；0/1 结算及异常分支使用明确标注的固定输入。示例策略只依赖报价，真实天气策略及天气修订路径随阶段 A 迁移后验收。

## 文件与复现

- `reproducible-evidence.zip`：不可变自采数据集、请求、各次运行的输入日志及净值 CSV。
- `account-and-runs.json`：版本、运行配置、最终快照、恢复与确定性核对。
- `trading-desktop.png`、`trading-mobile-order.png`、`trading-mobile-account.png`、`backtest-compare.png`、`backtest-curves.png`、`live-disabled.png`：浏览器证据。
- 启动、数据导出、systemd 和回滚流程见 [TRADING_WORKBENCH.md](../../TRADING_WORKBENCH.md)。解压数据集至 `<trading-root>/datasets/` 后，可在回测页选择同一哈希并按保存参数重跑。

测试服务仅绑定 `127.0.0.1:8518`。之前绑定 `0.0.0.0` 的尝试被自动审批拒绝，已使用本机回环地址继续验收。

## 提交与复查

用户明确授权自行验收、审核并提交后，隔离分支已推送到原项目远端，并创建 [草稿 PR #36](https://github.com/RENEGADES20/nice-weather/pull/36)。GitHub 连接器创建 PR 返回 403，随后使用本项目已有 Git 凭据通过官方 GitHub API 完成；凭据未打印或持久化。真实资金接口保持关闭。

远端完整 CI 已启动，包含 Nautilus、lint、unit、fixture-dashboard、frontend（含原有 Playwright 场景）和 security；各提交检查结果以 [PR 检查页](https://github.com/RENEGADES20/nice-weather/pull/36/checks) 为准。本报告保留已执行本地证据与尚未完成的部署门槛，PR 保持草稿，未合并到 main。

复查修正后，原生交易测试 **31 项通过**，日志见 `review-pytest.txt`；前端类型检查通过。此前 111 项全套本地结果保留于 `pytest.txt`，不将旧日志改写成新版本结果。首轮 [CI #82](https://github.com/RENEGADES20/nice-weather/actions/runs/34701946062) 的 Nautilus、lint、unit、fixture-dashboard、security 通过；frontend 为 12 通过、2 失败，上述定位修正后通过后续提交重新运行。历史输入如曾受撤单顺序问题影响，升级重放将暂停并保留差异，禁止修改旧成交或重置现金。
