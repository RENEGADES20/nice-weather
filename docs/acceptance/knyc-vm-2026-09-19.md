# KNYC VM 部署记录

线上测量入口仍为同一终端，临时加 `?measure=1` 后，浏览器控制台仅输出 `terminal-performance` 的指标名和毫秒数；默认关闭日志输出。首屏口径为导航起点到快照/连接就绪后的绘制帧，切档为点击到绘制帧，盘口为收到 book 事件到绘制帧；服务端等待和交易所确认不能混为浏览器绘制时间。旧标记及时清理，单类 measure 只保留最后一项。此开关用于采集验收证据，不新增独立诊断入口。

## PR #51 写盘修复与重启验收

2026-09-19 18:37 UTC 左右，合并提交 `4060512720c24ed51fd06b88890d8cfed3f8e8c3` 已原位部署；CI run `35461245784` 七项通过。包 415,577 bytes，SHA256 `ce324372338901f092b716bcb422ad3db6b35b0d327d0ba87264087f7cc1ca50`，65 个运行文件逐个校验通过；未创建环境或历史版本副本。当前记录为 `/var/lib/nice-weather-knyc/deployment-current.json`。

六个 KNYC 服务 active、NRestarts=0。terminal/feed/HRRR/backtest/Kalshi Paper/Poly US Paper PID 分别为 1502346/1502348/1502349/1502347/1502350/1502351。旧 collector/dashboard/sandbox/backtest PID 保持 1092517/1188046/1160080/1160081。Paper 冷启动时曾出现约 44 秒旧快照；后续两账户更新时间分别约 0.11/0.30 秒，现金各 100、成交 0，游标继续推进，空账户恢复通过。不能将 systemd active 单独当作业务已恢复。

同为五秒采样，feed 的 write_bytes 从 2,973,696 变为 1,671,168；系统 journal 写盘从 1,716,224 变为 245,760；两个 Paper 合计从 2,764,800 变为 5,685,248。全库读取并行进行且事件吞吐不同，这些单次样本不构成性能 p95 或整体吞吐改善比例。后续 vmstat 两个一秒样本仍有 72%/75% I/O wait，VM 性能验收未通过。

用户批准的 Logs Writer / Monitoring Metric Writer 已添加至现有 VM 服务账户，后续十五分钟检查没有 PermissionDenied。全库只读 quick_check 的长读快照阻止主库 WAL 回收，WAL 达到约 997 MB、剩余空间约 2 GB 后，已终止且仅终止该只读进程（1498518）。未完成主库完整性核验，旧数据库快照的已批准删除尚未执行；不得写成已释放这部分空间。中止原因记录在 VM 的 `integrity-check-current.json`，后续需受控校验方案，禁止直接重启同类无界扫描。

公网自动重连暴露前端恢复缺陷：旧 502 文案保留，连接恢复没有刷新随服务器重启轮换的 CSRF。故障注入进一步发现较早快照引发图表异常、卸载整个 React 页面。修复已通过本地构建和两项终端回归，包含过期快照禁止下单及未确认请求原 ID 恢复；正在按 PR 流程发布，不能提前记为线上通过。最新合成六档/每档 1,500 点验收：初载单次 479 ms，切档/本地反馈/收到行情至显示的 24 样本 p95 分别为 65.5/49.5/32.4 ms，交易所延迟未测量。生产模型、Live 适配、规则审计、线上 p95、全市场日观察仍未完成。

## PR #50 历史分页

合并提交 `abe66becc04ce25cea932565ad82e4c10227461f` 已通过七项 CI 并原位部署。图表历史初载从 1,500 行改为 200 行顶档投影，完整 L2 原文仍在采集库。双平台初载与继续分页通过；VM 单次查询为约 10–23 ms、18 KB，缓存与并行负载不同，不能当作 p95。

以下 PR #49 和首次安装内容为各阶段历史证据，当前版本以上方最新记录为准。

## PR #49 原位更新与公网入口

合并提交 `8ac85c7740460dceee1cedeba378d8502ea8319d`，CI run `35458106315` 七项通过。发布包 415,340 bytes，SHA256 `e0f91600ea7c2faa7068b75ebc341788704c60bcef02be0c0d2dc68d05374d07`。现役目录与 Python 环境原位复用，目录名仍含首次版本 59e0b3e，实际代码以 manifest 为准；未新增整套版本或环境。

只重启 terminal（PID 1499434），健康接口返回上述完整提交；feed/Paper PID 保持，均 active、NRestarts=0。未认证 snapshot 返回 401，认证模式 cloudflare。沿用已有 Cloudflare Access 应用及策略，服务端验证签名、issuer、audience、有效期；无新增密码。

Cloudflare Tunnel 第一条规则将 `^/(terminal/?|api/.*|assets/.*)$` 转发至回环 8767，其后旧通配入口保持 8501。浏览器访问 `/terminal` 显示连接正常、Kalshi 六档、天气及模拟账户。图表历史请求出现超时；VM 首批 1,500 行读取实测 8.649 秒、未压缩约 1.43 MB，当时全库完整性扫描并行运行。该结果说明历史冷读需要减小首批分页，不能单独归因于框架或网络，也不构成完整性能验收。

以下是首次安装的历史证据，公网与认证状态已由本节替代。

检查日期：2026-09-19 UTC。这是部署与有界运行证据，完整产品、线上性能、市场日及实盘验收尚未完成。

## 版本与产物

- 目标：项目 `project-be5e00e4-0ddc-40cf-ba8`，`us-west1-b`，`nice-weather-vm`。
- 运行提交：`59e0b3e32f3cdb5ea29801ddf346a32de88481c9`；PR #47 已合并，CI run `35453137500` 七项通过。
- 发布包：`knyc-release-59e0b3e.tar.gz`，414,295 bytes；SHA256 `9d2596c34cb9b7f3a110af7018aaafd3817e21176d3283e78eb3002d3acbf65e`。
- 安装器：`install_knyc_release_59e0b3e.py`；SHA256 `979b8ea14810465d23ae9211f8aae3071672ad8c8509d6a2a30edce303a44f5c`。
- SSH 上传后校验两文件哈希；解包后校验 manifest 的 64 个文件。安装任务于 16:42:11 UTC 成功退出，状态 0。
- 目录：`/opt/nice-weather/knyc-releases/59e0b3e32f3cdb5ea29801ddf346a32de88481c9`，符号链接 `/opt/nice-weather/knyc-current`；独立 Python 3.12.3 环境，Nautilus 1.231.0。
- 新状态目录 `/var/lib/nice-weather-knyc`，0750；新增服务 UMask=0077。版本目录内 `deployment-record.json` 记录实际安装时间、版本、服务及 Live=false。

## 运行核验

新增 feed、HRRR、backtest、terminal、Paper Kalshi、Paper Poly US 六个服务均 active，初次检查自动重启次数为 0。`http://127.0.0.1:8767/health` 返回 ok 和完整运行 SHA，未认证 snapshot 返回 401。

16:44 UTC 左右，采集库有 1,033 个事件；Kalshi、Poly US、METAR、NWS、CLI index、HRRR 健康状态均为 connected，各来源最新事件距检查时间约 1.3–108.4 秒。不同来源周期不同，该快照不证明持续无中断。

两个 Paper 账户均 running，现金各 100.0，成交 0。单独重启 Kalshi Paper 后，账户继续 running，现金 100.0、成交 0，最新快照年龄为 0.0 秒。此检查仅覆盖空账户恢复；不能代替有挂单、持仓、部分成交及未知状态的恢复验收。

旧 collector/dashboard/sandbox/backtest PID 分别仍为 1092517/1188046/1160080/1160081，未因新部署重启；旧运行清单 fc83189 及其数据库保留。未切换公网路由、未下真实订单、未删除历史数据。

安装后系统盘约 2.3 GB 可用，使用率 92%；内存可用约 1,765 MB，无 swap。采集 1 GiB 余量保护仅覆盖新采集写入，完整市场日验收前仍需评估全 VM 数据增长。

## 入口与剩余工作

公网 `niceweather.trade` 仍指向旧 Streamlit 8501；新服务仅监听回环 8767，候选 `/terminal`、`/api/*`、`/assets/*` 路由尚未配置。沿用现有 HTTPS 和访问控制，不能为预览放宽保护。

安装器未创建终端密码，新增应用登录保持关闭。此前要求用户新设密码的步骤已撤回：用户原来未设置此密码，明确要求不要新增无依据的认证流程。配置辅助脚本仅上传到 VM，未执行、未创建或修改凭据。接下来只读核验现有 Cloudflare Access，确定保持现有保护的认证复用方式后再实施。

当前没有经验证的 KNYC 生产模型、完整规则核验、Native Live 适配及用户账户/风险配置。真实合约继续 no-trade。公网交互性能、完整市场日与完整故障恢复仍待验证，不能由单次健康检查或合成行情测试替代。
