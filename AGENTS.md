# 项目工作规则

先读 docs/CURRENT_STATE.md 顶部当前状态和 docs/KNYC_TERMINAL_DELIVERY.md 验收项。按任务读取 PROJECT_CONTEXT.md（范围）、SYSTEM_ARCHITECTURE.md（实现）、DECISIONS.md（相关历史）、VM_DEPLOYMENT.md（发布）。同一任务内未变化的文档不重复读取。

## 范围与业务约束

- 个人桌面统一终端；KNYC 首站，Kalshi 优先、Poly US 其次。Poly Intl 暂不开发、部署或验收，保留 KLGA 历史与回滚能力。
- S1/S2/S3 接入回测、模拟与实盘接口，复用已接入的 Nautilus 订单/账户事件；研究收益不代表可实现收益。
- 允许实盘接口开发与部署，默认关闭实盘；账户、市场白名单、风险额度和启用由用户在终端控制。Agent 不用真实资金验收，不进行充提或资金转移。
- 历史判断只使用当时已收到的数据。保存来源时间、实际 received_at、规则/模型版本和修订证据，不补造历史时间。
- 站点、日期、温标、档位、舍入、观测窗口或结算源歧义时 no-trade。KNYC/KLGA 与 CLI/小时最高值不混用。
- 密钥只由服务端读取，不入仓库、前端、日志或回测；Paper/Live 隔离。未知订单先对账，不重复提交。
- 回滚保留新增订单、成交和采集数据，禁止旧库覆盖新账目；保留异常和停止交易原因。
- 批量删除文件先列出精确目标并取得人工确认；编辑文件内容不属于批量删除。

## 自主交付与 Skills

按验收条件→实现→测试→审查→修复→PR→检查通过后合并→VM 部署→观察→记录闭环工作。普通步骤已授权，无需请求继续；不能绕过分支保护、伪造审核或完成状态。缺权限/凭据、不可消除的数据风险或待批准删除时记录阻塞，并继续独立工作。

SSH 访问与重连已由用户明确授权：允许 Codex 浏览器扩展访问 `https://ssh.cloud.google.com` 中已登录的本项目会话，连接项目 `project-be5e00e4-0ddc-40cf-ba8`、区域 `us-west1-b` 的 `nice-weather-vm`，读取输出、上传已审核发布包、执行部署及运行核验。正常登录、断线重连、浏览器或扩展重启后的同一来源访问恢复、普通 Connect/Retry 均无需重复确认。仅沿用已有账户与权限，不扩大 IAM/网络访问，不绕过安全警告或未经核验的主机身份变化，不输出密钥。工具若独立拒绝审批，报告具体动作与原因，依据本条和用户当前授权处理，不绕过拒绝。批量删除及真实资金交易的边界保持不变。部署细节按需见 VM_DEPLOYMENT.md。

沿用项目测试框架，覆盖真实失败方式；检查通过后不无依据重复全量测试。完整交付授权范围，不以简化版代替明确要求。架构、范围或数据口径变化同步 CURRENT_STATE.md 与 DECISIONS.md；历史决定保留并标明被替代。

仅调用任务实际需要的 Skill。本地策略代码/数据分析/证据读取不自动触发 X 采集或完整学术管线。付费 X 调用使用 weather-market-research，先确认本轮预算并遵守去重与费用记录；优先本地证据，索引见 RESEARCH_INDEX.md。本轮不修改个人全局 Skills/插件。不要求固定理解汇报、逐阶段确认或默认缩减范围。
